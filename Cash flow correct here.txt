Stage 1 — Architecture & Ingestion Workflow (Pristine Start & State Machine)Objective: Ensure the dashboard opens in a 100% pristine empty state (no hard-coded or pre-fetched static metrics) and establishes a robust state machine that holds the UI in a loading state until all dependent processing stages are complete.Analysis Needed:Examine how index.html boots and why loadDashboardData() previously auto-fetched ./dashboard_data.json and ./insights.json on startup.Audit the event handling sequence when uploading files to ensure no partial UI updates occur before all three templates (Income Statement, Balance Sheet, Cash Flow Statement) are fully parsed.Project Files Required:index.html (New Baseline)Verification Method:Open index.html in a clean browser window $\rightarrow$ verify that all KPI cards, chart canvases, alert cards, and SWOT boxes are completely hidden/empty (window.dashboardData = null).Upload 1 or 2 files $\rightarrow$ verify that the dashboard stays in an explicit "Awaiting remaining files..." state without rendering incomplete charts.Success Criteria: Zero financial data rendered on open; UI transitions from empty to loading to fully rendered only after all three files are processed.Checkpoint: Present Stage 1 Root Cause & Solution Plan for your confirmation before generating Stage 1 code.
Stage 2 — Calculation Engine Parity & Balance Sheet Cash Correction (Fixing Low Cash Ratio)Objective: Correct the Balance Sheet extraction logic in the calculation layer so that Cash, Current Assets, and Current Liabilities are properly accumulated across all bank accounts, eliminating the false "Low Cash Ratio (0.00)" warning while maintaining 100% metric accuracy.Analysis Needed:Trace the row-by-row extraction loops in Python calculate_dashboard.py and the client-side JavaScript calculation engine.Identify why row-overwriting occurred for accounts where category 01 == "cash" vs. accounting item == "current Assets".Project Files Required:data.jsondashboard_data.jsonPython calculate_dashboard.pyVerification Method:Compare raw bank account balances in data.json against the computed cash and cash_ratio in dashboard_data.json.Success Criteria: cash_ratio evaluates to its true, non-zero financial ratio (e.g., $0.82$ instead of $0.00$), and Current Assets/Liabilities accumulate all sub-accounts correctly.Checkpoint: Present Stage 2 Root Cause & Solution Plan for your confirmation before modifying calculation code.
Stage 3 — Multi-Series Expenditure Growth Trends Chart Restoration (Fixing Issue 1)Objective: Restore complete visibility of all 10+ expenditure category growth trend lines alongside Revenue in opexChart.Analysis Needed:Trace growth_trends.categories generation in dashboard_data.json and client-side processing.Inspect Chart.js dataset binding, color palette assignment, legend toggle handlers, and scale formatting in index.html to determine why expenditure lines disappeared.Project Files Required:dashboard_data.jsonindex.htmlVerification Method:Inspect charts.opex.data.datasets array length in browser dev tools $\rightarrow$ verify it contains Revenue + all expenditure categories.Success Criteria: opexChart renders Revenue (bold brand line) alongside all individual expenditure category lines (cost of food, people cost-employee, capacity costs, etc.) with an interactive, toggleable legend.Checkpoint: Present Stage 3 Root Cause & Solution Plan for your confirmation before generating Stage 3 code.
Stage 4 — Rule Engine Expansion & Option C SWOT Integration (Fixing Issue 2)Objective: Eliminate sparse/empty findings and restore the full depth of insights generated by the complete Python Rule Engine framework (Revenue, Profitability, Expense, Balance Sheet, and Cash Flow rules), applying Option C ("Current-Year SWOT + Multi-Year Evidence") without empty SWOT boxes.Analysis Needed:Analyze why the previous client-side JavaScript engine only evaluated 4 basic rules instead of the 15+ rules in the Python backend (profitability_rules.py, expense_rules.py, balance_sheet_rules.py, cashflow_rules.py, revenue_rules.py).Audit renderDynamicInsights(y) in index.html to ensure year-filtering cleanly displays point-in-time SWOT summaries while retaining multi-year evidence trails.Project Files Required:insights.jsonalert_swot_rules.pyconfig.jsonindex.htmlVerification Method:Verify that switching year tabs (FY2023, FY2024, FY2025) renders 6+ detailed, prioritized business alerts and populates all four SWOT quadrants (Strengths, Weaknesses, Opportunities, Threats) with rich evidence text.Success Criteria: Full parity with the manual Python rule engine output; zero empty SWOT quadrants.Checkpoint: Present Stage 4 Root Cause & Solution Plan for your confirmation before generating Stage 4 code.
Stage 5 — Revenue Allocation Capacity Chart Overlay RedesignObjective: Redesign revTrendChart so Total Revenue is drawn as a wider background capacity/shadow bar, with expenditure categories stacked proportionally within and overlaid on the Revenue bar at the same x-axis position, featuring a single unified legend.Analysis Needed:Configure Chart.js dataset properties: stack: 'capacityGroup' (Revenue, order: 2, barPercentage: 0.85) vs. stack: 'allocationGroup' (Expenditures, order: 1, barPercentage: 0.55).Remove all redundant manual HTML legend DOM containers.Project Files Required:index.htmldashboard_data.jsonVerification Method:Visual inspection of revTrendChart $\rightarrow$ verify Revenue forms a wider background frame around the narrower internal expenditure stack.Success Criteria: Visual clarity communicating "Total Revenue Capacity $\rightarrow$ Internal Expenditure Allocation" with exactly one clean legend.Checkpoint: Present Stage 5 Root Cause & Solution Plan for your confirmation before generating Stage 5 code.